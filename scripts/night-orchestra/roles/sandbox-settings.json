{
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Edit",
      "Write",
      "WebSearch",
      "WebFetch",
      "Bash"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Edit(./.env)",
      "Edit(./.env.*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // empty' | grep -qiE '\\.env|git push|gh (issue|pr|api|repo)' && { echo 'BLOCKED (night-orchestra sandbox): this role may not read .env, push git, or write to GitHub' >&2; exit 2; }; exit 0"
          }
        ]
      }
    ]
  }
}
