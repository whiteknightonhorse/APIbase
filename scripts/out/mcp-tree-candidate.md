# MCP tree candidate — statistics (T-0183)

Source: disputes/0180-smithery-naming-score-98-to-100.ruling-1.md §4 item 2, corrected per
disputes/0183-mcp-tree-candidate-generator.ruling-2.md (attempt 2 treated any mcpName root
whose provider differed from the toolId provider as "categorical", which wrongly counted
single-provider renames like razorpay<-razorpayifsc and single-provider "category-shaped"
words like currency<-exchangerate as roots. This attempt requires >=2 DISTINCT providers
already under a root before reusing it, and only falls back to `category` for providers
with no such qualifying root).
Generated by `npx tsx scripts/gen-mcp-tree-candidate.ts`. Candidate only — does not touch
mcpName, DB, or tool-definitions.ts. Full old→new table: mcp-tree-candidate.csv.

- tools: 1437
- roots (total): 43 — gate ≤ ~50: PASS
  - roots reused from existing categorical mcpName roots: 30 (90 providers, 273 tools)
  - roots from `category` field fallback (no existing categorical root): 23 (323 providers, 1164 tools)
- distinct (root.provider) 2nd-level nodes: 413
- single-leaf 2nd-level nodes: 28
- single-tool providers (baseline, structurally unavoidable singletons): 28
  — gate single-leaf nodes ≤ single-tool providers: PASS
- duplicate new mcpNames (must be 0): 0
- providers spanning >1 existing categorical root, re-homed to their primary one: 0
- roots with exactly 1 provider (singleton chains, no branching): 3 (device, messaging, podcasts)
- category-field values with exactly 1 provider (baseline, structurally unavoidable): 3
  — gate singleton-root chains ≤ that baseline: PASS
- observation (not a gate, left as-is per ruling): providers whose name equals their root, e.g. weather -> weather.weather.get_current: 13 providers, 67 tools (anime, books, crypto, device, education, email, finance, health, jobs, music, news, sports, weather)

Overall: PASS
